// src/types/index.ts

export interface IPerson {
  name: string;
  // Add other properties as per the SWAPI response
}

export interface IShip {
  name: string;
  // Add other properties as per the SWAPI response
}
